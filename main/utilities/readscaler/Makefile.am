bin_PROGRAMS=readscaler

common_cxxflags=-I$(shell root-config --incdir)
common_ldflags=$(shell root-config --libs)

readscaler_SOURCES=readscaler.cpp
readscaler_CXXFLAGS=$(common_cxxflags)
readscaler_LDFLAGS=$(common_ldflags)

